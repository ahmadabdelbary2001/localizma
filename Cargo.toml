# LOCALIZMA MODIFICATION NOTICE: This file was modified from the LocalSend upstream source to remove Flutter-specific workspace members and establish the Localizma Rust workspace.
[workspace]
resolver = "3"
members = [
    "cli",
    "packages/core",
    "server",
]

# Profiles are only honored in the workspace root manifest.

# Full debuginfo dominates target/ size; line tables keep backtraces usable.
[profile.dev]
debug = "line-tables-only"

# RSA key generation is bignum-heavy and takes ~10x longer unoptimized;
# keep the crypto crates optimized in dev so tests stay fast.
[profile.dev.package.rsa]
opt-level = 2

[profile.dev.package.num-bigint-dig]
opt-level = 2
